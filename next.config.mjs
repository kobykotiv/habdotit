/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cdn.carbonads.com'], // Add your image domains here if needed
  }
};

export default nextConfig;
